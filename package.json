{
  "name": "positronic",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "positronic": "./dist/cli.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "swc . --out-dir dist && chmod +x dist/cli.js",
    "start": "npm run build && node dist/example.js",
    "test": "jest --silent"
  },
  "dependencies": {
    "jest": "^29.7.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@swc/cli": "^0.6.0",
    "@swc/core": "^1.10.4",
    "@swc/plugin-transform-imports": "^6.0.4",
    "@types/jest": "^29.5.14",
    "ts-jest": "^29.2.5",
    "typescript": "^5.0.0"
  }
}
